import React from 'react';
import { MagicLink } from '@/client/pages/MagicLink';

export const MagicLinkPage = () => {
  // TODO: Check if there is an email persisted in browser storage that we can use to
  //       populate the input (via the email prop)
  return <MagicLink />;
};
