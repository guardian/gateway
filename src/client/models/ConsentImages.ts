import { EVENTS_IMAGE } from '@/client/assets/consents';
import { Consents } from '@/shared/model/Consent';

export const CONSENT_IMAGES: Record<string, string> = {
	[Consents.EVENTS]: EVENTS_IMAGE,
};
